/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  trailingSlash: true,
  //i18n: {
  //  locales: ["da", "en"],
  //  defaultLocale: "en",
  //},
};

export default nextConfig;
